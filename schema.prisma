generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
}

// Your Epstein database schemas will go here
// Example:
// model Document {
//   id        String   @id @default(cuid())
//   title     String
//   createdAt DateTime @default(now())
//   updatedAt DateTime @default(now())
// }